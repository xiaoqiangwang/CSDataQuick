csdq_add_qml_module(
    URI
        "CSDataQuick.Data"
    PLUGIN_TARGET
        csdataplugin
    CLASS_NAME
        CSDataPlugin
    DEPENDS
        Qt${QT_VERSION_MAJOR}::Core
        Qt${QT_VERSION_MAJOR}::Qml
        csdata
    SOURCES
        sortfilterproxymodel.h
        sortfilterproxymodel.cpp
        plugin.h
        plugin.cpp
    QML_FILES
        qmldir
        plugins.qmltypes
)
